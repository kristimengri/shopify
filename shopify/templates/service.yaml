apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopify.fullname" . }}
  labels:
    {{- include "shopify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopify.selectorLabels" . | nindent 4 }}
